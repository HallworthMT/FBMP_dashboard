{
    "name": "uiFBMP",
    "version": "0.0.1",
    "description": "Simple UI for FBMP Dashboard",
    "license": "MIT",
    "scripts": {
        "start": "pm2 start --env prod",
        "start-prod": "pm2 start --env prod",
        "start-production": "pm2 start --env prod",
        "start-dev": "pm2 start --env dev_local",
        "start-dev-local": "pm2 start --env dev_local",
        "start-dev-remote": "pm2 start --env dev_remote",
        "stop-all": "pm2 stop all"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.7",
        "express": "^4.21.2",
        "nvm": "^0.0.4",
        "path": "^0.12.7",
        "vhost": "^3.0.2"
    }
}
